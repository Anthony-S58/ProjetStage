{% extends 'base.html.twig' %}

{% block title %}Chocolaterie{% endblock %}

{% block body %}
    <h1>Chocolaterie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chocolaterie.id }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ chocolaterie.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chocolaterie_index') }}">back to list</a>

    <a href="{{ path('chocolaterie_edit', {'id': chocolaterie.id}) }}">edit</a>

    {{ include('chocolaterie/_delete_form.html.twig') }}
{% endblock %}
