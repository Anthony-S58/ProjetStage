{% extends 'baseadmin2.html.twig' %}

{% block title %}Employe
{% endblock %}

{% block body %}
	<h1>Créer un nouvel employé</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ employe.id }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('admin_employe_index') }}">Retour à la liste</a>

	<a href="{{ path('admin_employe_edit', {'id': employe.id}) }}">Modifier</a>

	{{ include('admin_employe/_delete_form.html.twig') }}
{% endblock %}
