{% extends 'baseadmin2.html.twig' %}

{% block title %}Employe
{% endblock %}

{% block body %}
	<h1>Employe</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ employe.id }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('admin_employe_index') }}">back to list</a>

	<a href="{{ path('admin_employe_edit', {'id': employe.id}) }}">edit</a>

	{{ include('admin_employe/_delete_form.html.twig') }}
{% endblock %}
